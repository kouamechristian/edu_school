{{ form_start(form) }}
    <div class="row">
        <div class="col-md-6 mb-3">{{ form_row(form.isRepeating) }}</div>
        <div class="col-md-6 mb-3">{{ form_row(form.boursier) }}</div>
    </div>
    <div class="d-flex justify-content-between mt-4 pt-3 border-top">
        <a href="{{ path('admin_registration_index') }}" class="btn btn-light"><i class="fas fa-arrow-left me-1"></i> Retour</a>
        <button type="submit" class="btn btn-success"><i class="fas fa-save me-1"></i> Enregistrer</button>
    </div>
{{ form_end(form) }}
